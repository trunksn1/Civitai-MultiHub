# Community feeds

Public directory of shareable MultiHub feed configs. To use one, download the `.multihub.json`
file and load it via **Import feed** in the extension.

To publish your own feed: export it from the extension, add the file to this folder with a
descriptive name (e.g. `flux-landscapes.multihub.json`), and open a pull request.
